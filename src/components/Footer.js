import React from 'react'

const Footer = () => {
  
  return (
    <div>
      Show
      <button>All</button>
      <button>Active</button>
      <button>Complete</button>
    </div>
  )
}

export default Footer